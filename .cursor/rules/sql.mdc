---
description: SQL 数据库开发规范
globs: ["**/*.sql"]
alwaysApply: false
---

# SQL 编码规范

## 格式规范
- SQL 关键字全部大写（`SELECT`、`FROM`、`WHERE`、`JOIN`）
- 表名、列名全部小写，使用下划线分隔（如 `user_profiles`、`created_at`）
- 每个子句独占一行，缩进对齐
- 多表 JOIN 时，每个 JOIN 独占一行
- 复杂查询添加注释说明业务目的

## 查询规范
- 禁止使用 `SELECT *`，必须明确列出所需字段
- WHERE 条件中的列必须有索引，避免全表扫描
- 分页查询使用 `LIMIT + OFFSET` 或游标分页（大数据量时）
- 禁止在 WHERE 中对列使用函数（如 `WHERE YEAR(created_at) = 2025`），会导致索引失效
- 子查询优先改写为 JOIN（性能更优）

## 索引规范
- 频繁查询的字段必须建立索引
- 联合索引遵循最左前缀原则，高区分度的列放在前面
- 禁止在低区分度列（如 `gender`、`status`）上单独建索引
- 定期清理无用索引，索引过多会降低写入性能

## 事务与并发
- 多步写操作必须包裹在事务中（`BEGIN` / `COMMIT` / `ROLLBACK`）
- 事务尽可能短，避免长事务导致锁等待
- 使用乐观锁（版本号）或悲观锁（`SELECT ... FOR UPDATE`）处理并发更新
- 禁止在事务中执行外部调用（HTTP 请求、消息队列发送）

## 安全
- 所有查询必须使用参数化（Prepared Statement），禁止字符串拼接
- 禁止使用 `DROP TABLE` / `TRUNCATE` 等危险操作，除非经过审批
- 数据库账号遵循最小权限原则，应用账号禁止 GRANT ALL
- 敏感字段（手机号、身份证）使用应用层加密存储

## DDL 规范
- 每张表必须有主键（推荐自增 `BIGINT` 或 `UUID`）
- 每张表必须包含 `created_at`、`updated_at` 时间戳字段
- 软删除使用 `deleted_at` 字段，禁止物理删除业务数据
- 字段必须有默认值或允许 NULL（明确选择，不可遗漏）
- 字段注释必须说明业务含义和取值范围
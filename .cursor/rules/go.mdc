---
description: Go 语言云原生与微服务开发规范
globs: ["**/*.go"]
autoAttach: true
---

# Go 编码规范

## 错误处理
- Go 没有异常，必须显式检查每一个返回的 `error`，禁止使用 `_` 忽略错误。
- 错误信息应包含上下文，使用 `fmt.Errorf("failed to fetch user %s: %w", id, err)` 包装错误。
- 仅在顶层（如 main 或 HTTP Handler）记录错误日志，底层仅负责返回错误。

## 接口与并发
- 接口由使用方定义，且应保持精简（通常 1-3 个方法）。
- 优先使用 Channel 进行 Goroutine 间通信，避免直接共享内存。
- 启动 Goroutine 必须明确其生命周期与退出机制，防止 Goroutine 泄漏。

## 命名与结构
- 导出名称使用大驼峰，非导出名称使用小驼峰。
- 接收者（Receiver）命名应简短（1-2 个字母），且同一结构体的所有方法接收者名称必须一致。
- 避免使用 `init()` 函数，除非有明确的包级初始化需求。
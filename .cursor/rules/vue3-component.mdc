---
description: Vue 3 组件开发编码规范
globs: ["**/*.vue"]
alwaysApply: false
---

# Vue 3 组件编码规范

## 单文件组件结构
- 必须使用 `<script setup lang="ts">` 语法糖，禁止使用 Options API（除非维护旧项目）
- 文件内部顺序固定：`<template>` → `<script setup>` → `<style scoped>`
- `<style>` 必须添加 `scoped` 属性，防止样式污染；需要全局样式时使用 `:deep()` 穿透

## script setup 内部代码顺序
按以下顺序组织代码，形成统一阅读路径：
1. 外部依赖引入（vue、第三方库）
2. 内部模块引入（组件、composables、store、api）
3. Props / Emits 定义（`defineProps` / `defineEmits`）
4. 组合式函数调用（`useXxx()`）
5. 组件专属响应式状态（`ref` / `reactive`）
6. 计算属性（`computed`）
7. 方法定义
8. 生命周期钩子（`onMounted` / `onUnmounted` 等）
9. `defineExpose`（仅在需要时）

## 命名规范
- 组件文件名：PascalCase（如 `UserProfile.vue`）
- 文件夹名：复数形式（如 `components/`、`composables/`）
- 组合式函数：`use` 前缀 + PascalCase（如 `useAuth.ts`）
- 事件名：kebab-case（如 `@update:model-value`）
- Props：camelCase（如 `modelValue`）

## Props 与 Emits
- Props 必须使用 TypeScript 泛型定义，禁止使用运行时声明（`defineProps(['title'])`）
- 可选 Props 必须标注 `?`，并提供合理默认值
- Emits 必须使用 TypeScript 泛型定义，声明每个事件的参数类型
- 禁止直接修改 Props，必须通过 `emit('update:xxx')` 通知父组件

## 模板规范
- 禁止 `v-if` 和 `v-for` 同级使用（`v-for` 优先级更高，会导致性能问题）
- `v-for` 必须绑定唯一且稳定的 `:key`，禁止使用 `index` 作为 key（列表会变动时）
- 模板中禁止写复杂表达式，超过一个运算符的逻辑必须提取为 `computed`
- 使用语义化 HTML 标签（`<article>`、`<section>`、`<nav>`），避免全部使用 `<div>`

## 响应式使用
- 原始类型（string/number/boolean）统一使用 `ref`
- 对象类型优先使用 `ref`（团队一致性 > 便利性），除非明确不会整体替换才用 `reactive`
- 解构 `reactive` 对象时必须使用 `toRefs()`，否则丢失响应式
- 禁止滥用 `watch`，能用 `computed` 解决的不要用 `watch`

## 组件拆分原则
- 单文件组件超过 200-300 行时，必须考虑拆分
- 可复用逻辑提取为 Composable（`composables/useXxx.ts`），Composable 只负责逻辑不返回 UI
- 如果逻辑只在一个组件内使用，直接写在 `<script setup>` 中，不要过度拆分
- 禁止混用 Options API 和 Composition API

## 样式规范
- CSS 类名使用 BEM 命名法（`.block__element--modifier`）
- 颜色、间距、字号等设计令牌统一从 CSS 变量或 Tailwind 配置中读取，禁止硬编码
- 过渡动画使用 Vue 内置 `<Transition>` / `<TransitionGroup>` 组件
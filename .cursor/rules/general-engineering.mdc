---
description: 跨语言通用工程与安全底线规范
globs:
alwaysApply: true
---

# 通用工程规范

## 安全底线
- 绝对禁止在代码中硬编码任何密钥、Token、密码或连接字符串。
- 所有外部输入（API 参数、用户表单）必须视为不可信，进行严格的校验与过滤。
- 涉及用户隐私数据（如手机号、身份证）在日志中必须进行脱敏处理。

## 测试要求
- 核心业务逻辑的单元测试覆盖率不得低于 80%。
- 测试用例命名必须清晰表达测试意图（如 `Test_ShouldReturnError_WhenUserNotFound`）。
- 禁止在单元测试中依赖外部真实服务，必须使用 Mock 或 TestContainer。

## 提交与重构
- 每次代码变更应保持原子性，单一职责，避免混合多个不相关的修改。
- 重构代码时，必须先补充测试用例，确保行为不变后再进行结构优化。
---
description: Rust 系统级开发编码规范
globs: ["**/*.rs"]
alwaysApply: false
---

# Rust 编码规范

## 代码格式与工具链
- 必须使用 `rustfmt` 格式化代码，CI 中检查格式一致性
- 必须通过 `clippy` 检查，禁止 `#[allow(clippy::all)]` 全局忽略
- 使用 `cargo` 管理依赖，`Cargo.toml` 中锁定关键依赖版本

## 错误处理
- 业务函数返回 `Result<T, E>`，禁止在库代码中使用 `unwrap()` 或 `expect()`
- 自定义错误类型使用 `thiserror` 派生宏，错误信息包含上下文
- 错误传播使用 `?` 运算符，配合 `anyhow` 处理应用层错误
- 禁止 `panic!()` 出现在生产代码路径中（测试代码除外）

## 所有权与生命周期
- 优先传递引用（`&T`）而非克隆（`.clone()`），减少不必要的内存分配
- 需要共享所有权时使用 `Arc<T>`，需要内部可变性时使用 `Rc<RefCell<T>>` 或 `Arc<Mutex<T>>`
- 生命周期注解仅在编译器要求时添加，不要过度标注
- 避免在公共 API 中暴露具体生命周期，使用 `'_` 或省略规则

## 类型系统
- 使用强类型封装业务概念（Newtype 模式），如 `struct UserId(u64)` 而非裸 `u64`
- 枚举（`enum`）优先于布尔标志和魔法数字
- 泛型约束使用 `where` 子句提高可读性
- Trait 实现必须为 Trait 中的每个方法编写文档注释

## 并发
- 优先使用 `tokio` 异步运行时处理 I/O 密集型任务
- 禁止在 `async` 函数中执行阻塞操作，必须使用 `tokio::task::spawn_blocking`
- Channel 通信优先使用 `tokio::sync::mpsc`，避免共享可变状态
- `Send + Sync` 约束必须在文档中明确说明

## 模块与项目结构
- 每个模块（`mod`）对应一个文件或目录，保持单一职责
- 公共 API 通过 `pub use` 在 `lib.rs` 中重新导出，隐藏内部实现细节
- 测试代码放在同文件的 `#[cfg(test)] mod tests` 中，集成测试放在 `tests/` 目录
- 示例代码放在 `examples/` 目录，作为活文档

## 安全
- 禁止在 `unsafe` 块外使用不安全操作，`unsafe` 块必须有注释说明安全不变量
- 尽量减少 `unsafe` 代码量，封装在最小范围内并提供安全 API
- 序列化/反序列化使用 `serde`，禁止手动解析不可信数据
---
description: 项目通用编码规范
globs:
alwaysApply: true
---

# 通用编码规范

## 代码注释要求
- 所有公共函数/方法必须添加注释，说明功能、参数和返回值
- 复杂逻辑必须添加行内注释解释意图
- 类和接口必须有文档注释
- 注释使用中文，代码使用英文

## 代码风格
- 变量和函数命名要有意义，禁止使用 a、b、temp 等无意义命名
- 单个函数不超过 50 行，超过则拆分为子函数
- 避免嵌套超过 3 层，使用提前返回（early return）减少嵌套
- 禁止提交 console.log / print 等调试语句

## 错误处理
- 所有异步操作必须有 try-catch 错误处理
- 错误信息要有意义，包含上下文信息
- 不要吞掉异常（空 catch 块）

## 安全性
- 用户输入必须做校验和过滤
- 禁止在代码中硬编码密码、密钥等敏感信息
- SQL 查询必须使用参数化查询，禁止字符串拼接

## 修改原则
- 只修改与需求相关的代码，不要改动无关代码
- 修改前先理解现有代码逻辑，保持风格一致
- 不要删除已有的注释和文档